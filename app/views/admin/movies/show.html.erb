<%=@movie.id %><br>
<%= @movie.name %><br>
<%= @movie.is_showing %><br>
<%= @movie.image_url%>

  <h2>上映スケジュール</h2>
<% @schedules.each do |schedule|%>
  <%= "上映開始時刻：#{schedule.start_time}" %>
  <%= "上映終了時刻：#{schedule.end_time}" %>
  <%= link_to "スケジュール詳細", admin_schedule_path(schedule.id) %>
<% end %>


<%= link_to "スケジュールを追加する", new_admin_movie_schedules_path(@movie.id) %>

